import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Navbar } from "@/components/layout/Navbar";
import { Footer } from "@/components/layout/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Edit Text in Image/Screenshot Online for Free (AI-Powered) | PhoText",
  description:
    "The Easiest way to Edit Text in Image/Screenshot, Click on any Text in Image to Edit it, without needing PS skills, with our AI-Powered Image Text Editor. Support JPG, PNG, JPEG, WEBP, BMP.",
  keywords: [
    "edit text in image",
    "screenshot text editor",
    "replace text in screenshot",
    "image text editor",
    "photext",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        <Navbar />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
